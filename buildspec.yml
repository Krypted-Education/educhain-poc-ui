version: 0.1
environment_variables:
  plaintext:
    S3_BUCKET_NAME: "poc-1.educha.in"
phases:
  install:
    commands:
      - echo Uploading!
      - aws s3 cp . s3://${S3_BUCKET_NAME} --recursive
artifacts:
  files:
    - '**/*'
  base-directory: '*'
  discard-paths: yes